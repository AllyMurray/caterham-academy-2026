{
  "name": "caterham-academy-2026",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check script.js && npm test",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
